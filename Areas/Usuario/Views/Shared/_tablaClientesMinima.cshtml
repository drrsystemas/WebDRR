@model List<ViewCliente>
@{
    Boolean vendedor = (Boolean)ViewData["Vendedor"];

}

@{
    if (Model?.Count() > 0)
    {
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Nombre
                    </th>
                    <th>
                       Dni/cuit
                    </th>
                    <th>

                    </th>


                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    String dato = "[" + item.ClienteID + "] " + item.RazonSocial;

                    <tr>
                        <td>
                            @dato
                        </td>

                        <td>
                            @Html.DisplayFor(modelItem => item.NroIdentificacion)
                        </td>

                        <td>
                            <button onclick="seleccionarRegistro('@item.EntidadSucId', '@item.RazonSocial', '@item.NroIdentificacion', '@item.CorreoElectronico', '@item.ClienteID')"  class="btn btn-sm btn-success">Sel.</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
}
